/* Write your global styles here, in PostCSS syntax */
@tailwind base;
@tailwind components;
@tailwind utilities;

.grecaptcha-badge {
	visibility: hidden;
}

.map-center-control {
	display: flex;
	justify-content: center;
	align-items: center;
	width: 34px;
	height: 34px;
	background-color: #fff;
	border: 2px solid rgba(0, 0, 0, 0.2);
	background-clip: padding-box;
	border-radius: 4px;
}

.map-center-control:hover,
.map-center-control:focus {
	background-color: #f4f4f4;
}

.map-center-control:focus {
	outline: 2px solid #0066cc;
	outline-offset: 2px;
}

.map-center-control img {
	cursor: pointer;
	width: 25px;
}

/* Map Status Legend Control */
.map-status-legend {
	background-color: white;
	padding: 6px 10px;
	border-radius: 4px;
	border: 2px solid rgba(0, 0, 0, 0.2);
	background-clip: padding-box;
	box-shadow: 0 1px 5px rgba(0, 0, 0, 0.15);
	user-select: none;
	margin-bottom: 4px;
}

.map-status-legend-item {
	display: flex;
	align-items: center;
	gap: 6px;
}

.map-status-legend-item:not(:last-child) {
	margin-bottom: 2px;
}

.map-status-legend-icon {
	width: 14px;
	height: 14px;
}

.map-status-legend-title {
	font-size: 10px;
	font-weight: bold;
	color: #666;
	text-transform: uppercase;
	letter-spacing: 0.5px;
	margin-bottom: 4px;
	padding-bottom: 3px;
	border-bottom: 1px solid #e5e5e5;
}

.map-status-legend-label {
	font-size: 11px;
	line-height: 1.2;
	color: #333;
	font-weight: bold;
}

.stwui-button:focus-visible,
a[role='button']:focus-visible {
	box-shadow:
			0 0 0 1px hsl(var(--primary-content)), /* In case the button being shadowed is --primary color */
			0 0 0 3px hsl(var(--primary)) !important;
}

.stwui-textarea:focus-visible,
.stwui-input:focus-visible,
.stwui-select-display:focus-within, /* Need focus-within and !important for dropdowns */
input:focus-visible,
textarea:focus-visible {
	box-shadow:
			0 0 0 2px hsl(var(--primary)) !important;
}

/* Ensure focus ring is visible on all interactive elements */
[tabindex]:focus-visible:not([tabindex='-1']) {
	outline: 2px solid hsl(var(--primary));
	outline-offset: 2px;
}
