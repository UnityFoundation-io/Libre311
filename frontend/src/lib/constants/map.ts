/**
 * Map configuration constants
 */

/** Default pan delta for keyboard navigation (arrow keys) - finer control for precise positioning */
export const KEYBOARD_PAN_DELTA_FINE = 20;

/** Larger pan delta for general map browsing */
export const KEYBOARD_PAN_DELTA_COARSE = 80;

/** Zoom level when a service request marker is selected (17 = street-level detail) */
export const SELECTION_ZOOM_LEVEL = 17;
